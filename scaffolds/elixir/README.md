# ElixirScaffold

## Installation

First, [install Elixir](https://elixir-lang.org/install.html#unix-and-unix-like).

Then:

```bash
# fetch dependencies
mix deps.get
# to compile
mix compile
# to test
mix test
```
